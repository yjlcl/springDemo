eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8769
spring:
  application:
    name: zuuldemo
zuul:
  routes:
    api-a:
      path: /api-a/**
      serviceId: ribbonhystrixdemo
    api-b:
      path: /api-b/**
      serviceId: feigndemo